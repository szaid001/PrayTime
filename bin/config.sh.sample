#!/bin/bash

PROJECT_NAME=BOGUS

# -- SERVER ---
SERVER_USER=me
SERVER_ADDR=$PROJECT_NAME
SERVER_PATH=/www/$PROJECT_NAME
IDENTITY_FILE=~/.ssh/$SERVER_ADDR.pem

# --- DATABASE ---
DB_USER=db-user
DB_PASS=****
DB_NAME=$DB_USER

# --- LOCAL --- 
LOCAL_PATH=~/Projects/$PROJECT_NAME
LOCAL_ADDR=$PROJECT_NAME.local

# --- RSYNC ---
RSYNC_OPTIONS="--archive --verbose --human-readable --progress --recursive --copy-links --delete"
